CREATE TABLE Wish (
    id INTEGER NOT NULL PRIMARY KEY,
    text TEXT NOT NULL
);

insertWish:
INSERT INTO Wish(id, text)
VALUES(?, ?);

removeAllWishes:
DELETE FROM Wish;

removeWish:
DELETE FROM Wish WHERE id = ?;

selectAllWishesInfo:
SELECT *
FROM Wish;
